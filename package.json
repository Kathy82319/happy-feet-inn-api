{
  "name": "happy-feet-inn-api",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "build": "esbuild src/index.js --bundle --outfile=dist/_worker.js --format=esm && cp -r public/. dist/",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  },
  "dependencies": {
    "jose": "^5.0.0",
    "uuid": "^9.0.1"
  },
  "devDependencies": {
    "esbuild": "^0.21.4",
    "wrangler": "^4.0.0"
  }
}