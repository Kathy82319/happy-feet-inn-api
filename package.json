{
  "name": "happy-feet-inn-api",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "build": "esbuild src/index.js --bundle --outfile=dist/_worker.js --format=esm",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  },
  "dependencies": {
    "jose": "^5.0.0"
  },
  "devDependencies": {
    "esbuild": "^0.21.4",
    "wrangler": "^4.0.0"
  }
}